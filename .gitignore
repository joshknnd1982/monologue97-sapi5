# Build trees and staged output
build_x86/
build_x64/
output/
dist/

# The Monologue '97 engine is First Byte's copyrighted work and ships only in
# the Releases installer. See "Building from source" in README.md for how to
# populate this from a Monologue '97 CD.
engine/

# Rendered proof-of-life audio (regenerate with make_samples)
samples/

# Scratch build artifacts from the standalone probe scripts
*.obj
*.exe
*.dll
*.ilk
*.pdb
*.exp
*.lib
*.raw
*.wav

# --- not part of this project ---------------------------------------------
# This working directory is reused between speech-engine projects, so files
# from earlier ones can still be sitting here. They are left on disk untouched
# and simply kept out of this repository.

# The original Monologue '97 CD (First Byte's copyrighted material).
bin/

# Leftovers from the BestSpeech / Outloud wrappers that shared this tree.
src/IEnumSpObjectTokensImpl.*
src/ISpDataKeyImpl.*
src/ISpTTSEngineImpl.*
src/b32_wrapper.*
src/bestspeech_sapi.def
src/bestspeech_server.cpp
src/com.*
src/debug_log.h
src/pipe_client.*
src/pipe_protocol.h
src/registry.*
src/sapi_main.cpp
src/utils.hpp
src/voice_attributes.hpp
src/voice_token.*
installer/innosetup_code.iss
installer/post_process_iss.cmake
